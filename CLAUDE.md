# 전역 규칙

## 대화 형식
- 두괄식 — 결론·결과를 첫 줄에 먼저, 근거는 그 다음
- 핵심과 근거는 개조식 — 문단 서술 대신 항목당 한 줄
- 근거는 빼지 않되 한 줄로 압축. 세부는 물었을 때만 푼다
- 줄표(—) 금지. 쉼표나 마침표로 끊는다
- 번역투·AI 상투어 금지("조용히 실패한다", "지뢰", "~하는 것을 확인" 류). 평범한 한국어 단어로 쓴다

## 말과 일
- 원인을 말하기 전에 그 원인을 가리키는 숫자를 먼저 낸다. 없으면 "아직 모른다"고 말한다
- 변수는 하나씩 바꾼다. 두 개를 같이 넣으면 어느 쪽이 먹었는지 영영 모른다
- 예고하지 말고 한다 — 하고 나서 결과를 보고한다
- 코드 작업은 karpathy-guidelines 스킬의 원칙을 따른다

## 문서 — 지식은 압축해서 남긴다
- 일기장식 md 금지. 서사 로그 대신 결론만: 제약("X 금지 — 이유 한 줄"), 결정(ADR: 결정/맥락/이유/대가/재검토 조건), 상태(덮어쓰기)
- 원본은 한 곳. 같은 사실을 두 파일에 적지 않는다 — 두 곳에 있으면 갈린다
- 경고문보다 긍정 라우팅 — "X 하지 마라"보다 "고칠 자리는 Y다"
- 히스토리는 git이 담당한다. md가 히스토리를 흉내내지 않는다
- 완료된 작업 지시서·세션 로그는 남길 지식만 본문 문서로 옮기고 지운다 (git에 남는다)
- 파일 상한: 라우팅 인덱스 30줄 · 상태 15줄 · 결정 40줄. 넘으면 나누라는 신호다. docs/는 무제한, 대신 첫 3줄에 요약
- 리포에 이미 문서 규약이 있으면 그것을 따른다 — 새 구조를 덮어씌우지 않는다

## 프로젝트 메모리
- 프로젝트 지식의 정본은 리포 안(CLAUDE.md·docs/·memory/)이다. auto-memory에는 개인 취향·기기 요령·리포로 가는 포인터만 남긴다
