---
name: commit
description: git 커밋 메시지 생성. 커밋을 만드는 모든 순간에 이 스킬을 쓴다 — 사용자가 커밋을 요청했을 때, 작업 마무리로 커밋이 필요할 때, git commit을 실행하기 직전. 메시지를 직접 쓰지 말고 이 절차로 만든다. 트리거 — "/commit", "커밋해줘", "커밋 메시지", "commit and push", 그 외 git commit이 일어나는 모든 작업.
---

# commit — 커밋 메시지는 haiku가 쓴다

장황한 커밋의 원인은 세션 맥락이다. 작업한 모델은 과정을 다 알아서 다 적으려 한다.
그래서 메시지는 맥락 없는 haiku 서브에이전트가 diff만 보고 쓴다. 서사가 들어갈 통로가 없다.

## 형식

- 제목: `type(scope): 요약`. 명령형, 50자 이내, 마침표 없음
- type: feat fix refactor perf test docs build chore 중 하나. scope는 생략 가능
- 언어: 리포의 최근 커밋 언어를 따른다. 없으면 한국어. type과 scope는 항상 영어
- 본문: 기본 생략. diff로 알 수 없는 "왜"(제약, 수치 근거, 트레이드오프)가 있을 때만 1~3줄
- 금지: 변경 파일·함수 나열, "이 커밋은/This commit" 서사, diff 재서술, 구현 상세
- 하니스가 붙이라는 푸터(Co-Authored-By 등)는 검사 대상이 아니다. 메시지 뒤에 그대로 붙인다

## 절차

1. 스테이징을 확정한다. `git status`와 `git diff --staged`. 비어 있으면 먼저 add
2. "왜" 힌트를 정한다. 이유가 diff만으로 자명하지 않을 때만 한 줄, 자명하면 없음.
   힌트에는 이유만 담는다. 무엇을 바꿨는지는 diff가 말한다
3. Agent 도구를 model "haiku"로 호출한다. 프롬프트는 아래 템플릿에 diff와 힌트를 끼운다.
   diff가 2000줄을 넘으면 `git diff --staged --stat` 전체 + 핵심 파일 diff로 줄인다
4. 반환된 메시지를 위 형식으로 검사한다. 위반은 직접 고친다. haiku를 다시 부르지 않는다
5. 최근 커밋 언어와 다르면 요약만 번역한다. 그 메시지로 커밋한다

## haiku 프롬프트 템플릿

```
git 커밋 메시지를 작성하라. 커밋 메시지 텍스트만 출력한다.
설명·인사·코드블록·도구 사용 금지.

규칙:
- 1행: "type(scope): 요약". type은 feat|fix|refactor|perf|test|docs|build|chore,
  scope는 생략 가능. 요약은 한국어 명령형, 50자 이내, 마침표 없음
- 본문: 기본적으로 쓰지 않는다. 아래 "왜 힌트"가 있을 때만 빈 줄 하나 뒤에 1~3줄로,
  힌트의 이유만 서술한다
- 변경 파일 나열, diff 재서술, "이 커밋은" 같은 서사 금지

diff:
<git diff --staged 출력>

왜 힌트: <한 줄 또는 "없음">
```
